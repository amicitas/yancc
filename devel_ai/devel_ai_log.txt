This file includes AI generated code (Gemini)

# Development Log - AI Coding Sessions

2026-03-15: Initial setup & Session Logging Mandate
- Created 'devel_ai' directory.
- Created 'features_request.md' for feature tracking.
- Created 'devel_ai_log.txt' for session logging.
- Updated 'GEMINI.md' to include a new mandate for providing full diffs.
- Researched PyCharm plugin diff shortcut: `Ctrl + O` (letter 'O') for expanding/collapsing diffs.
- Updated 'GEMINI.md' to include a mandate for automatic session logging to 'devel_ai/devel_ai_log.txt' when the user says "thank you" or closes the session.

2026-03-16: Better I/O & Run Handling
- Created `yancctools_io.py` to centralize I/O for options (JSON/YAML), inputs (HDF5), and results (HDF5).
- Refactored `yancctools.py` and `__init__.py` to use `yancctools_io.py`.
- Developed "Part 3" notebook (`2026-03-16 - Development of tools... - Part 3 - radial scan IO.ipynb`) focusing on radial scan I/O and data reloading.
- Created `yancctools_tests.py` for automated minimal testing of the radial scan + I/O workflow (using `ai_test_` run IDs).
- Simplified AI identifiers in headers/logs to just 'Gemini'.
- Updated `GEMINI.md` to clarify the AI Usage Disclaimer format.
- Bug Fixes:
  - Fixed `ImportError` in `yancctools.__init__`.
  - Filtered out `output_path` in `yancctools.py` to prevent `solve_dke` failures.
  - Fixed `date11time` typo in `yancctools.py`.
--- Session Log: Sat Mar 21 22:23:33 EDT 2026 ---
Updated scan_ambipolar_profile to return HDF5-compatible list structure (scan_results) instead of rho-keyed dict.
Updated yancctools_plot.py to iterate over scan_results.
Updated yancctools_tests.py to reflect new structure.
Created notebooks/2026-03-21 - Development of tools to help with comparison of yancc to sfincs - Part 4 - radial scan IO.ipynb.

Updated features_request.md and GEMINI.md feature tracking instructions.

--- Session Log: 2026-03-22 ---
- Fixed a kwargs handling bug in `yancctools.py` where `solve_dke` failed due to unexpected arguments like `nx` and `na`.
- Changed options handling from a negative filter (dropping known yancctools options) to a positive filter (extracting only explicitly known `solve_dke` options like `p1`, `p2`, `rtol`, etc.).
- Added the required `This file includes AI generated code (Gemini)` disclaimer to `yancctools.py`.
- Verified changes by successfully running `pytest yancc/yancctools/yancctools_tests.py`.

--- Session Log: 2026-03-22 (Convergence Scan Tool) ---
Implemented the automated convergence scan tool based on the plan in `temp/convergence_scan_plan.md`.

1. `yancc/yancctools/yancctools_convergence.py`:
   - Created the module with the `run_convergence_scan` function to automatically orchestrate scans over numerical parameters (`nx`, `na`, `nt`, `nz`).
   - The scan iterates across odd integer resolutions (starting from a minimum of 5) stepping backwards to roughly -50% and up to +100% of nominal values. 
   - Tracking is enabled through standard `runid` functionality, and metrics tracked during each `solve_dke` step are aggregated under HDF5 compatible dictionaries (tracking the resulting `<particle_flux>`).

2. `yancc/yancctools/yancctools_plot.py`:
   - Implemented `plot_convergence_scan` using Plotly to plot multi-panel convergence charts. It displays the convergence tracks of particle fluxes for all scanned parameters inside a single interactive dashboard.

3. `yancc/yancctools/__init__.py` & Tests:
   - Exported the newly created functions.
   - Added a `test_convergence_scan` unit test in `yancc/yancctools/yancctools_tests.py` verifying functionality at minimal resolutions. All tests ran and successfully passed.

4. Demonstration Notebook:
   - Generated `notebooks/2026-03-22 - Development of tools to help with comparison of yancc to sfincs - Part 5 - convergence scan.ipynb`.
   - Outlines how to initialize an equilibrium, set species density/temperatures, define the baseline options, run the convergence scan, output metrics, and plot.

5. `devel_ai/features_request.md`:
   - Updated the convergence scan feature entry to "Done".

--- Session Log: 2026-03-23 (Improved Convergence Scan Plotting) ---
Tool: Claude (via OpenCode)

1. Created `CLAUDE.md`:
   - Wrote project rules file for Claude-based AI tools, mirroring GEMINI.md with
     additions: Architecture Notes, Testing commands, Project Structure, expanded
     Coding Standards (Black, isort, NumPy docstrings). Removed Gemini-specific
     "Full Diffs" rule.

2. `yancc/yancctools/yancctools_convergence.py`:
   - Added `identify_species_name()` utility that maps Species objects to
     human-readable names (Electron, Hydrogen, etc.) by mass/charge lookup table.
   - `run_convergence_scan()` now stores additional metadata in results dict:
     `species_names`, `nominal_values`, `rho`, `erho` -- all HDF5-compatible.
   - Fixed `generate_scan_range()` to always include the nominal value in the
     scan range. Previously, nominal values like na=35, nt=13, nz=25 were not
     guaranteed to appear as scan points, causing empty deviation plots.
   - Reformatted with Black-compatible style, sorted imports with isort.
   - Updated AI disclaimer to include Claude.

3. `yancc/yancctools/yancctools_plot.py`:
   - Rewrote `plot_convergence_scan()` with the following improvements:
     * Layout changed to one row per parameter, two columns: absolute particle
       flux (left) and percent deviation from nominal (right).
     * Species names from results dict used in legends instead of "Species 0".
     * Dashed vertical lines mark the nominal resolution on each subplot.
     * Percent deviation calculated as (flux - flux_nominal) / |flux_nominal| * 100%.
     * Consistent styling: plotly_white template, hovermode="x unified",
       coordinated color scheme across species.
     * Title includes runid, rho, and erho for context.
     * Function now returns the Plotly figure object; new `show` parameter
       controls whether fig.show() is called.
     * Backward compatible: handles old results dicts without new keys via
       .get() defaults, and uses np.interp fallback when nominal value is
       not an exact scan point.
   - Fixed nominal flux lookup: uses int() cast for type-safe comparison
     (handles numpy int64 from HDF5 round-trips).
   - Updated AI disclaimer to include Claude.

4. `yancc/yancctools/__init__.py`:
   - Added `identify_species_name` to exports.

5. `devel_ai/features_request.md`:
   - Added "Improved convergence scan plotting" feature entry (Pending).

Key findings:
   - The original `generate_scan_range` with linspace + odd rounding often
     missed the nominal value (e.g., na=35 produced scan points [17, 43, 71]).
     This caused empty deviation plots for na, nt, nz while nx=5 worked by
     coincidence (5 happened to be included in its own range).
   - Species objects in yancc have no name attribute; identification requires
     reverse-lookup by mass (in proton masses) and charge (in elementary charges).
